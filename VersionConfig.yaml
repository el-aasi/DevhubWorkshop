mode: ContinuousDelivery
branches:
  master:
    regex: ^latest$
  pull-request:
    tag: pr
  feature:
    regex: ^feature?[/-]
    tag: useBranchName
    increment: Minor
    source-branches: ["master"]
